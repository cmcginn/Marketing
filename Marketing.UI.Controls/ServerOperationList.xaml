<UserControl x:Class="Marketing.UI.Controls.ServerOperationList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" Background="White">
    <ListBox  Name="serverControlListBox" ItemsSource="{Binding Screen.ServerOperations,Mode=TwoWay}" BorderBrush="{x:Null}" BorderThickness="0">
      <ListBox.ItemTemplate>        
        <DataTemplate>
          <Button Content="{Binding OperationName}" Click="Button_Click" />          
        </DataTemplate>
      </ListBox.ItemTemplate>
      <ListBox.ItemContainerStyle>
        <Style TargetType="ContentControl">
          <Setter Property="BorderThickness">
            <Setter.Value>0</Setter.Value>
          </Setter>
        </Style>
      </ListBox.ItemContainerStyle>
    </ListBox>
  </Grid>
</UserControl>
